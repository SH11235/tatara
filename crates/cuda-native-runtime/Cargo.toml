[package]
name = "cuda-native-runtime"
version = "0.5.0"
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }
authors = { workspace = true }
rust-version = { workspace = true }
publish = false

[features]
default = []
# Compiles CUDA C++ sources to an embedded fatbin; host backend selection belongs to consumers.
native-cuda = []

[lints]
workspace = true

[dependencies]
thiserror = "2"

[dev-dependencies]
gpu-kernels = { workspace = true }
